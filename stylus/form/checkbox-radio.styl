$checkbox-radio
	cursor: pointer
	display: inline-block
	line-height: 1.25
	position: relative
	input
		cursor: pointer
	&:hover
		color: $input-hover-color
	&[disabled],
	fieldset[disabled] &
		color: $input-disabled-color
		cursor: not-allowed

_checkbox()
	@extend $checkbox-radio

	{block}

_radio()
	@extend $checkbox-radio
	& + {$radio-selector}
		margin-left: 0.5em

	{block}

if !$silent.form.checkbox && !$silent-mode
	{$checkbox-selector}
		_checkbox()

if !$silent.form.radio && !$silent-mode
	{$radio-selector}
		_radio()
